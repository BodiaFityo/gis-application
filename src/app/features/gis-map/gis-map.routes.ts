import {Route} from '@angular/router';
import {GisMapComponent} from './components/gis-map.component';

export const gisMap: Route[] = [
    {
        path: '',
        component: GisMapComponent,
    },
];
